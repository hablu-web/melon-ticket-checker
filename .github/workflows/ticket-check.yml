name: Melon Ticket Checker

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: 

jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check Tickets
        uses: mooyoul/melon-ticket-actions@v1.1.0
        with:
          product-id: 211782
          schedule-id: 100003
          seat-id: FLOOR H
          slack-incoming-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          message: '<@U05PET1FKFT> 달려달려~'
